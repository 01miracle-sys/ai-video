# 2026-07-13 工作日志

## 完成了什么

1. **项目目录重构**：从散装脚本 → 规范的模块化结构

2. **重构语音识别模块** `测试/transcribe.py` → `src/asr/transcriber.py`
   - 抽取为 `WhisperTranscriber` 类（延迟加载模型、可配置参数）
   - 添加 `TranscribeResult` NamedTuple 返回结构化结果
   - 保留 `format_timestamp()` 工具函数

3. **重构笔记生成模块** `generate_notes.py` → `src/notes/generator.py`
   - 抽取为 `NotesGenerator` 类（可配置模型/API/温度/分段大小）
   - 保留原有逻辑（长文本分段 → 逐段摘要 → 合并生成）
   - 新增 `save()` 方法统一保存逻辑

4. **新增字幕烧录模块** `src/subtitle/burner.py`
   - `SubtitleBurner` 类封装 FFmpeg 调用
   - 支持自定义字体/大小/描边
   - 完整的错误处理（文件不存在/FFmpeg 未安装/超时）

5. **新增工具模块** `src/utils/file_utils.py`
   - `ensure_dir()`、`get_output_path()` 工具函数

6. **配置文件** `config/settings.yaml`
   - ASR / Notes / Subtitle / Output 四个模块的完整配置

7. **统一入口** `main.py`
   - `python main.py video.mp4` → 全流程（转文字 + 笔记）
   - `python main.py video.mp4 --burn` → 全流程 + 烧录字幕
   - `python main.py test.txt` → 仅生成笔记
   - 自动识别输入类型（视频或文本）

8. **项目基础设施**
   - `requirements.txt` (faster-whisper, requests, pyyaml)
   - `.gitignore`
   - `scripts/run.bat`（新入口批处理）
   - 更新了 `生成笔记.bat`（指向新入口）
   - 旧文件保留并标注为弃用

## 项目结构（重构后）

```
E:\AI-Video\
├── main.py                   # 统一入口
├── config/settings.yaml       # 配置文件
├── requirements.txt           # 依赖清单
├── .gitignore
├── scripts/run.bat            # 批处理入口
├── 生成笔记.bat               # 旧入口（已更新）
├── src/
│   ├── asr/transcriber.py     # 语音识别
│   ├── notes/generator.py     # 笔记生成
│   ├── subtitle/burner.py     # 字幕烧录
│   └── utils/file_utils.py    # 工具函数
├── outputs/                   # 输出目录
├── tests/                     # 测试目录
├── 测试/                      # 旧测试文件（保留）
└── ai-video/                  # 文档目录
```
